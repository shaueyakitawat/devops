apiVersion: apps/v1
kind: Deployment
metadata:
  name: news-service
  namespace: moneymitra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: news-service
  template:
    metadata:
      labels:
        app: news-service
    spec:
      containers:
        - name: news-service
          image: moneymitra-news-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: moneymitra-secrets
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: news-service
  namespace: moneymitra
spec:
  selector:
    app: news-service
  ports:
    - port: 8000
      targetPort: 8000
